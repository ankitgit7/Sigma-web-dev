ID are unique identifiers in any element. Same id cannot be used in multiple elements and one element cannot have multiple IDs.

One class can be used in multiple elements and one elements can have multiple classes as well.